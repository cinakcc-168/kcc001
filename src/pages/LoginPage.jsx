import {useState} from 'react';import {Navigate} from 'react-router-dom';import {useAuth} from '../context/AuthContext';export default function LoginPage(){const {session,signIn,error}=useAuth();const [email,setEmail]=useState(''),[password,setPassword]=useState(''),[msg,setMsg]=useState(''),[busy,setBusy]=useState(false);if(session)return <Navigate to="/dashboard" replace/>;async function submit(e){e.preventDefault();try{setBusy(true);setMsg('');await signIn(email,password)}catch(x){setMsg(x.message)}finally{setBusy(false)}}return <main className="login"><form className="card" onSubmit={submit}><div className="logo">T</div><p className="eyebrow">SECURE STAFF LOGIN</p><h1>Welcome to Tiny POS</h1><p className="muted">Sign in with the owner account from the new Supabase project.</p>{(msg||error)&&<div className="error">{msg||error}</div>}<label>Email<input type="email" value={email} onChange={e=>setEmail(e.target.value)} required/></label><label>Password<input type="password" value={password} onChange={e=>setPassword(e.target.value)} required/></label><button disabled={busy}>{busy?'Signing in…':'Log in'}</button></form></main>}
