import {useState} from 'react';import {NavLink,Outlet} from 'react-router-dom';import {Boxes,LayoutDashboard,LogOut,Menu,Settings,Store} from 'lucide-react';import {useAuth} from '../context/AuthContext';
export default function AppShell(){const {profile,shop,signOut}=useAuth();const [open,setOpen]=useState(false);return <div className="shell"><aside className={open?'side open':'side'}><div className="brand"><b>T</b><span>{shop?.shop_name||'Tiny POS'}</span></div><nav><NavLink to="/dashboard"><LayoutDashboard/>Dashboard</NavLink><NavLink to="/products"><Boxes/>Products</NavLink><NavLink to="/settings"><Settings/>Settings</NavLink></nav><button className="logout" onClick={signOut}><LogOut/>Log out</button></aside>{open&&<button className="backdrop" onClick={()=>setOpen(false)}/>}<main><header><button className="menu" onClick={()=>setOpen(true)}><Menu/></button><div><Store size={18}/> {profile?.branches?.name||'Main Branch'}</div><strong>{profile?.full_name||'Owner'} · {profile?.role}</strong></header><section className="content"><Outlet/></section></main></div>}
